{
  "name": "@pay/payment-runtime",
  "version": "0.1.0",
  "description": "Payment rail router for Solana agent commerce: Solana Pay, escrow, x402, Pay.sh, USDC, allowances, wallets, and payouts",
  "type": "module",
  "engines": { "node": ">=20" },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@pay/agent-runtime": "file:../agent-runtime"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0",
    "vitest": "^1.6.0"
  }
}
