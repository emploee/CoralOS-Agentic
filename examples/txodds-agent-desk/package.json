{
  "name": "@pay/example-txodds-agent-desk",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "description": "TxODDS Agent Desk — a Tauri operator console over the kit's proxy, run ledger, proof receipts, and payment rails. No bespoke glue: every byte comes from the existing HTTP APIs.",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "ui": "npx --yes serve ui -l 3030",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0"
  }
}
